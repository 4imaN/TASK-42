# The JAR is built inside Docker (multi-stage) — exclude host-side build output
# so a local ./mvnw package doesn't leak into the image context.
target/
*.log
.idea/
.vscode/
.claude/
.DS_Store
Thumbs.db
.env
.env.*
*.swp
*.swo
*~
docs/
sessions/
API_tests/
unit_tests/
Dockerfile.test
